{% if contract.structurePhpdoc %}{% include 'phpdoc/structure_phpdoc.twig' with {'structure_phpdoc': contract.structurePhpdoc} %}{% endif %}
interface {{ contract.name }}
    {{- contract.contracts is not empty ? ' extends ' }}
    {%- include 'collection/contract_collection.twig' with { 'contract_collection': contract.contracts } only %}
{
{% include 'collection/constant_collection.twig' with {
    'constant_collection': contract.constants
} only %}
{% if needs_line_after(contract, 'constants') %}

{% endif %}
{% include 'collection/methods/pure_virtual_collection.twig' with {
    'method_collection': contract.methods
} only %}
}
