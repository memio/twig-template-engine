{% if property.propertyPhpdoc %}{% include 'phpdoc/property_phpdoc.twig' with {'property_phpdoc': property.propertyPhpdoc} %}{% endif %}
{#- Trimming lines #}
    {{ property.visibility ~ ' ' }}
    {{- property.isStatic ? 'static ' -}}
    ${{ property.name -}}
    {{ property.defaultValue ? (' = ' ~ property.defaultValue)|raw }};
{#- Trimming lines -#}
